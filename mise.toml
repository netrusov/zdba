[tools]
java = { version = "openjdk-25" }
ruby = "jruby-10.0.4.0"

[tasks.setup]
description = "Setup project"
run = [
  "mise install",
  "bundle install",
]

[env]
JAVA_OPTS = "--enable-native-access=ALL-UNNAMED --sun-misc-unsafe-memory-access=allow"
